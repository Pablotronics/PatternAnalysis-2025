# Project 3 — 2D Context-Aware Network (CAN) for Prostate Segmentation

**Student:** Pablo Parra  
**ID:** 43159996  
**Course:** COMP3710 — Pattern Analysis (UQ, 2025)

---

## 1. Project Overview and Objectives

Welcome to my implementation of **Project 3**, which develops a **2D Context-Aware Network (CAN)** for medical image segmentation using MRI scans of the prostate.  
The dataset is the **HipMRI Study on Prostate Cancer**, consisting of 2D axial slices. The target metric is a **minimum Dice similarity coefficient (DSC) of 0.75** on the test set.

This project not only focuses on the technical implementation of pattern recognition but also simulates professional software development workflows: version control with **GitHub**, environment reproducibility via **Conda**, and package documentation for seamless portability.

I initially explored **Project 2 (U-Net)** but moved on to **Project 3**, which introduces multi-scale context aggregation through *context modules* that enhance boundary segmentation accuracy.  
The real challenge lay in making the repository fully self-contained, well-documented, and easily reproducible across machines.

---

# 1.2 How it all works in plain words - READ BEFORE TRYING ANYTHING

Here I will explain a few things needed ot understand the whole package, no code here, as this will be provided after this high level explanation.

### The virtual environment manager is conda
The whole packages has a LOT of dependencies and libraries. To make it as reproducible as possible in a broad range of machines, I decided to set up a method for a virtual conda environment. Why conda and not venv, because I read that conda manages pytorch and pytorch verisons automatically. Hence after conda is installed, it should automatically manage which pytorch and cuda and python version are right for anyones individual machine. THis should save the hassles of pip installing one thing and then finding out that you do't have the right version to be compatible with another thing. the installation of conda is standarised and should be done by following this: https://www.youtube.com/watch?v=AgnAs0nPEVg&t=10s

Once this is done, I have two yml file sin places which describe all the dependencies needed to run my implementation and these cna be run from bash, to install the whole environment. There are two environment files, I recommend using the environment-gpu.yml file, as this allows for intallation to be able to use CUDA GPUs. But there is a CPU only option (worse).

Alternatively, if you are feeling adventurous, and just want to install directly from pip without any virtual environment, the dependencies are also listed on the requirements.txt file. This is NOT RECOMMENDED!

### Source the data and one-time decompression

The model is trained on the prostate images which are sources from rangpur or the web, exactly as outlined in the task sheet. Check section 1.3.3 and references. In short, the easy way to get the data is to log into Rangpur and download the data from the folder. Now, all the photos come in their own compressed folders, which only contain the one photo. This meant that they needed to be decompressed, only once, so that they sit not inside their own zip folder. To do this, I wrote a script called: decompress_data.py. Run this first, with the right file path.

### Run train.py and then predict.py

As specified, there is no model uploaded into the repo. So OF COURSE, you must run the training step at least once before there prediction step. Then, you can just run the rpediction step. Note that every time the training step is ran, is overwrites the previous one.

### Gitignore in all things not specified to be in repo

Note that all things that should not be in the repo, are marked ni the git ignore file, such as the models, the models generated by the train.py and the predicitons generated by the predict.py.

### Training and Prediction arguments in terminal

Note that the train and rpedict scripts allow for the specification of parameters by passing arguments in the terminal. Indeed, they need to be run form the terminal, with passing some key attributes.

### Prediction GUI

Note that the prediction step while it exports the images, it also runs a GUI that allows for scrolling throgh the samples. Closing this GUI kills the running of the script.

---

## How to run (to follow in order)

### 🧩 Set up conda:

```powershell
# Clone the repository (this might change depending on your clone source)
git clone https://github.com/Pablotronics/PatternAnalysis-2025.git
cd PatternAnalysis-2025

# Install conda by following:
# https://www.youtube.com/watch?v=AgnAs0nPEVg&t=10s

# Create and activate Conda environment (GPU)
conda env create -f environment-gpu.yml
conda activate comp3710-gpu

# (Alternative CPU-only environment)
conda env create -f environment-cpu.yml
conda activate comp3710-cpu
```

---

### 💾 Download data and decompress

Download the dataset from Rangpur (see task sheet section 1.3.3), and place it in your chosen directory, for example:

```
C:\Users\User\Desktop\Final_Project_COMP3710\keras_slices_data
```

Then decompress all files by running:

```powershell
# From the project root
python .\P3_ProstateSegmentation_PabloParra\decompress_data.py --data_root "C:\Users\User\Desktop\Final_Project_COMP3710\keras_slices_data"
```

This will expand all `.nii.gz` images into their proper folders for training, validation, and testing.

---

### 🧠 Train for the first time

Before prediction, train the model at least once. Example command:

```powershell
# Run training (GPU environment recommended)
python .\P3_ProstateSegmentation_PabloParra\train.py --data_root "C:\Users\User\Desktop\Final_Project_COMP3710\keras_slices_data" --epochs 20 --batch_size 4
```

The model will save the best checkpoint automatically under:

```
P3_ProstateSegmentation_PabloParra\models\unet2d_can_hipmri_best_<timestamp>.pth
```

---

### 🔍 Run prediction step

Once trained, you can test and visualise predictions with:

```powershell
python .\P3_ProstateSegmentation_PabloParra\predict.py --data_root "C:\Users\User\Desktop\Final_Project_COMP3710\keras_slices_data" --save_pngs --num_samples 9
```

This produces prediction PNGs and metrics in:
```
P3_ProstateSegmentation_PabloParra\preds\
```

An interactive GUI will also appear, allowing you to scroll through the results.

---

## 1.3 High-Level Package Overview (Read Before Running)

### 🧩 Virtual Environment — Conda

This project has numerous dependencies.  
To ensure reproducibility, I used **Conda**, which automatically manages **PyTorch**, **CUDA**, and **Python** compatibility.  
After installing Conda (see [this guide](https://www.youtube.com/watch?v=AgnAs0nPEVg&t=10s)), simply use the provided environment files.

Two YAML files are included:
- `environment-gpu.yml` — Recommended (CUDA support)
- `environment-cpu.yml` — CPU fallback (slower)

Alternatively, `requirements.txt` lists all dependencies, but **direct pip installation is not recommended**.

---

### 📦 Data Setup & One-Time Decompression

The dataset (HipMRI prostate MRI slices) must be downloaded manually as per the task sheet.  
Once downloaded, decompress the data once using:

```bash
python P3_ProstateSegmentation_PabloParra/decompress_data.py
```

This extracts `.nii.gz` files into a clean folder structure:

```
keras_slices_data/
├── keras_slices_train/
├── keras_slices_seg_train/
├── keras_slices_validate/
├── keras_slices_seg_validate/
├── keras_slices_test/
└── keras_slices_seg_test/
```

---

### 🚀 Run Order

1. **Set up Conda environment**
2. **Download and decompress data**
3. **Train the model**
4. **Run predictions and visualise**

---

### ⚙️ GitIgnore

The `.gitignore` ensures all generated artifacts (e.g., models, prediction PNGs, logs) are excluded from the repository.

---

### 🖥️ Terminal Arguments

Both training and prediction scripts accept arguments such as:
- `--data_root`
- `--epochs`
- `--batch_size`
- `--save_pngs`
- `--num_samples`

---

### 🧭 Prediction GUI

Running the prediction script launches a simple GUI to scroll through prediction samples.  
Closing the GUI window terminates the script execution.

---

## 2. Environment Setup

A reproducible environment is provided using **Conda**.

```bash
# Clone repository
git clone https://github.com/Pablotronics/PatternAnalysis-2025.git
cd PatternAnalysis-2025

# Install Conda if not already installed:
# https://www.youtube.com/watch?v=AgnAs0nPEVg&t=10s

# Create and activate Conda environment (GPU)
conda env create -f environment-gpu.yml
conda activate comp3710-gpu

# (Alternative CPU-only environment)
conda env create -f environment-cpu.yml
conda activate comp3710-cpu
```

To verify your environment:

```bash
python -c "import torch; print('CUDA available:', torch.cuda.is_available()); \
print('Device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

---

## 3. Data Preprocessing

```bash
python P3_ProstateSegmentation_PabloParra/decompress_data.py
```

### Pipeline:
- Normalisation (z-score → min–max)
- Resize to 256×256
- Binary mask conversion (`prostate=3 → 1`, background=0)
- Dataset split (train/validate/test)

---

## 4. Model Training

Train from the `P3_ProstateSegmentation_PabloParra` directory:

```bash
python P3_ProstateSegmentation_PabloParra/train.py --data_root "./keras_slices_data"
```

| Parameter     | Value            |
| ------------- | ---------------- |
| Epochs        | 20               |
| Batch Size    | 4                |
| Learning Rate | 5e-4             |
| Optimizer     | Adam             |
| Loss Function | CrossEntropyLoss |

Checkpoint directory:

```
P3_ProstateSegmentation_PabloParra/models/
└── unet2d_can_hipmri_best_<timestamp>.pth
```

The best checkpoint is automatically selected based on validation Dice score.

---

## 5. Model Evaluation & Prediction

```bash
python P3_ProstateSegmentation_PabloParra/predict.py \
  --data_root "./keras_slices_data" \
  --save_pngs \
  --num_samples 9
```

Outputs:
- **Dice Coefficient** printed in terminal
- **Predicted masks** saved in `P3_ProstateSegmentation_PabloParra/preds/`
- **Optional NIfTI outputs** in `P3_ProstateSegmentation_PabloParra/preds_nii/`

To visualise interactively:

```python
from P3_ProstateSegmentation_PabloParra.utils import show_samples
show_samples(model, test_ds, device, k=3)
```

---

## 6. Example Results

**Quantitative:**
```
TEST Dice (prostate): 0.9186
```

**Qualitative:**

| Input MRI | Ground Truth | CAN Prediction |
|------------|---------------|----------------|
| ![](./docs/input_sample.png) | ![](./docs/gt_sample.png) | ![](./docs/pred_sample.png) |

---

## 7. Reproducibility Notes

- Set random seeds with `torch.manual_seed(42)`
- Deterministic mode ensures reproducibility across runs
- Automatically falls back to CPU if CUDA is unavailable

---

## 8. References

1. **Yu et al. (2015)** — *Multi-Scale Context Aggregation by Dilated Convolutions*. [arXiv:1511.07122](https://arxiv.org/abs/1511.07122)  
2. **Ronneberger et al. (2015)** — *U-Net: Convolutional Networks for Biomedical Image Segmentation*. [arXiv:1505.04597](https://arxiv.org/abs/1505.04597)

---

## 9. Author & Acknowledgements

Developed by **Pablo Parra (Pablotronics)**  
for *COMP3710: Pattern Analysis*,  
**The University of Queensland (UQ)**, 2025.  

Dataset and starter code courtesy of **Dr. Shekhar S. Chandra**.

